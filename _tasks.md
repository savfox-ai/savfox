# Execution Tasks

## Phase 1

- [ ] Add a shared gateway home-path helper module for repeated files under `savfox_home`, and migrate the duplicated callers in gateway runtime code.

## Phase 2

- [ ] Move skill config path normalization to a shared `savfox_core` helper and update the TUI skills flow to reuse it.

## Phase 3

- [ ] Remove the duplicated gateway e2e HTTP client helper and reuse the shared test helper.

## Phase 4

- [ ] Run targeted verification for the touched crates/files.
- [ ] Mark completed tasks in this file.
