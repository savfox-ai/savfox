{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
    "numTurns": {
      "description": "The number of turns to drop from the end of the session. Must be >= 1.\n\nThis only modifies the session's history and does not revert local file changes that have been made by the agent. Clients are responsible for reverting these changes.",
      "format": "uint32",
      "minimum": 0.0,
      "type": "integer"
    },
    "sessionId": {
      "type": "string"
    }
  },
  "required": [
    "numTurns",
    "sessionId"
  ],
  "title": "SessionRollbackParams",
  "type": "object"
}