#![allow(clippy::needless_continue, clippy::unwrap_used)]
#![allow(deprecated)]

// Single integration test binary that aggregates all test modules.
// The submodules live in `tests/suite/`.
mod suite;
